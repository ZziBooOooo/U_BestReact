import React from "react";
import Card from "../../UI/Card/Card";

import style from "./FoodList.module.css";

const FoodList = (props) => {
  return <Card className={style.foodlist}>FoodList</Card>;
};

export default FoodList;
